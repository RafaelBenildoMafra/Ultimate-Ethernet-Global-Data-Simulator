@page
@model EthernetGlobalData.Models.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Channel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Channel.ChannelName" class="control-label"></label>
                <input asp-for="Channel.ChannelName" class="form-control" />
                <span asp-validation-for="Channel.IP" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Channel.IP" class="control-label"></label>
                <input asp-for="Channel.IP" class="form-control" />
                <span asp-validation-for="Channel.IP" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Channel.Port" class="control-label"></label>
                <input asp-for="Channel.Port" class="form-control" />
                <span asp-validation-for="Channel.Port" class="text-danger"></span>
            </div>
            <!-- Add an empty div with margin below the last input field -->
            <div class="form-group mt-2">
                <!-- 'mt-3' adds margin-top of size 3 (adjust as needed) -->
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
